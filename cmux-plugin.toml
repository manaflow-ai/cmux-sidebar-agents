[plugin]
name = "agents"
kind = "sidebar"
version = "0.1.0"
description = "Monitor AI agents and notifications"

[run]
command = ["target/release/cmux-sidebar-agents"]

[build]
command = ["cargo", "build", "--release"]
